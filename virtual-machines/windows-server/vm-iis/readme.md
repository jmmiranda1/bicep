# Windows Server with IIS Web

Deploys a single or multiple Windows server virtual machines with IIS services installed.

## Overview

For this use case, sometimes multiple Windows web servers are required. This module sets a base template for deploying the infrastructure. 

It deploys the following.

- `N` amount of Windows server with IIS installed.
- A load balancer.
- An availability set.